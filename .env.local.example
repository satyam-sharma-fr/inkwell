# Neon PostgreSQL connection string
DATABASE_URL=

# Vercel Blob token (auto-set when linked via Vercel)
BLOB_READ_WRITE_TOKEN=

# bcrypt hash of your admin password
# Generate with: node -e "require('bcryptjs').hash('yourpassword', 10).then(console.log)"
ADMIN_PASSWORD=

# Random string for signing session cookies (min 32 chars)
SESSION_SECRET=
